# Proxaly Backend — Railway Deployment
# Railway auto-detects package.json and runs npm start

[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "node index.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
